# Build output
dist/
node_modules/
coverage/

# Scratch + captured artifacts, not application code
scratchpad/
.filmstrip/
.playwright-mcp/

# Design exploration mockups. Loose JSX that was never wired into the app and
# doesn't parse under the app's (non-JSX) parser config — 11 parse errors.
# Lint the app, not the sketchbook.
New Design Ideas/

# Generated
public/publicAssets/
supabase/.temp/

# Storybook build output — 130MB of bundled JS. Git-ignores it already, but
# eslint reads .eslintignore only, and linting it hangs the run for minutes.
storybook-static/
