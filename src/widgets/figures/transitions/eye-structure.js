/*
 * Eye structure: a transition (see TransitionFigure.vue), matched to
 * theopenbrain.org (v0.2.3). It plays as the reader scrolls between two
 * figures: `scrub` is the stretch of that scroll the Lottie runs over.
 * Nothing to edit; it has no words of its own.
 */
export default {
  id: "eye-structure-transition",
  name: "Eye structure (transition)",
  animationKey: "animationEyeStructurTransition",
  lottieVersion: "v0.2.3",
  scrub: {
    from: 0.1,
    to: 0.9,
  },
  fields: [],
  defaults: {},
};
