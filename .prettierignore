dist/
node_modules/
coverage/
package-lock.json
scratchpad/
.filmstrip/
.playwright-mcp/
New Design Ideas/
public/publicAssets/
docs/architecture/graph.json
supabase/.temp/

# The authors' original widget HTML, preserved byte-for-byte. The widget
# library renders these as the "Original" view precisely so Stuart and Arjun
# can confirm their maths and interaction survived the Vue port — reformatting
# them would rewrite the artefact that view exists to show unchanged. The Vue
# ports in src/views/ are the code we own, and those ARE formatted.
src/widgets/source/

# Agent tooling scratch (untracked; listed so local `prettier --check .`
# matches what CI sees).
.superpowers/

# Claude Code agent worktrees (local only)
.claude/worktrees/

# Chapter manuscripts converted from the authors' DOCX (scripts/seed/
# docx-to-markdown.mjs). The seed generator reads them verbatim, so the
# committed text must be byte-for-byte what the SQL was generated from.
content/
